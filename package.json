{
  "name": "backend",
  "version": "1.0.0",
  "description": "curd operation using node and aws",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "avnish",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1109.0",
    "dotenv": "^16.0.0"
  }
}
